DEBUT
    // Déclaration du tableau
    Entier n = 10
    Entier tableau[n]  // n est la taille du tableau
    
    // Remplir le tableau avec des valeurs
    POUR i ← 0 JUSQU’À N-1 FAIRE
        tableau[i]
    FIN POUR

    // Affichage du tableau non trié
    Afficher "Tableau non trié :"
    POUR i ← 0 JUSQU’À N-1 FAIRE
        Afficher tableau[i]
    FIN POUR

    // Tri à bulles
    POUR i ← 0 JUSQU’À n-2 FAIRE
        POUR j ← 0 JUSQU’À n-2-i FAIRE
            SI tableau[j] > tableau[j+1] ALORS
                temp ← tableau[j]
                tableau[j] ← tableau[j+1]
                tableau[j+1] ← temp
            FIN SI
        FIN POUR
    FIN POUR

    // Affichage du tableau trié
    Afficher "Tableau trié :"
    POUR i ← 0 JUSQU’À N-1 FAIRE
        Afficher tableau[i]
    FIN POUR
FIN
